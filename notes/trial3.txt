Structural Causal Modeling (SCM) with Time-Varying Topology
Protocol for Covariance-Weighted Node Consolidation

This architecture facilitates the transition of legacy sovereign nodes into a unified structural component (e.g., the 1999 Euro transition) within a Graph Neural Network (GNN). By performing Weight Surgery instead of re-initialization, we preserve historical learning and causal associations across structural breaks.
1. The Membership Transition (Structural Shift)

To model the "collapse" of N legacy nodes into M consolidated nodes (M<N), we define a Projection Matrix (P). This operator preserves the conservation of system flow across the temporal discontinuity.

Mathematical Mapping: The state transition at time t is derived from the state at t−1 via:
xt​=P⋅xt−1​

For the Euro transition:
P=(10​10​10​01​)

(Rows 1-3 merge to Index 0; Row 4 persists at Index 1)
2. Knowledge Transfer: Covariance-Weighted Projection

Standard "Centroid Fusion" (averaging weights) fails due to Signal Inflation. If parent currencies (DEM, FRF) are highly correlated, a simple sum double-counts their shared variance, drowning out independent signals like the USD.

The Solution: Generalized Least Squares (GLS) Projection We project parent weights into the new subspace while "whitening" shared information using the Inverse Covariance Matrix (Σ−1). This ensures the child node inherits the "Principal Components" of its ancestors.
Wchild​=(PΣPT)−1PΣWparents​

Structural Benefits:

    Information De-duplication: Prevents collinearity from biasing the new node toward redundant parent signals.

    Variance Preservation: Ensures the child node maintains the "economic direction" of the parent cluster without "mellowing out" the signal.

3. Optimizer State Alignment (Momentum Warping)

To avoid Shape Mismatch in Adam optimizer buffers, the internal Momentum (mt​) and Velocity (vt​) must be warped to match the new topology.

Warping Formula:
vt(new)​=P⋅vt(old)​

By manually mapping legacy inertia into the new node, the optimizer treats the child node's velocity as a continuation of its parents' history, preventing catastrophic loss spikes during the transition.
4. Differentiable Transition (The Crossover Gate)

To prevent local network instability, we introduce a Sigmoid Gate (σ) as a temporal crossfader at the transition boundary τ. This makes the structural break differentiable, allowing the model to learn the optimal transition path.
Output(t)=(1−σ(t−τ))⋅Pathlegacy​+σ(t−τ)⋅Pathchild​


use batchnorm instead of layernorm since its time series prediction and alpha needs to remain similar to prevent vanishing or exploding gradient. 

BatchNorm only fixes the activations. It doesn't fix the Jacobian of the transition. 
Knowledge Transfer does not result in a chaotic feedback loop, we must bound the Lipschitz Constant (L) of the transition. We achieve this by enforcing a constraint on the Spectral Norm of the Projection Matrix (P).

The Condition: For the network to remain stable (non-explosive), the maximum singular value of the projection must satisfy:
σmax​(P)≤1

If σmax​(P)>1, the transformation is expansive, meaning the "Knowledge Transfer" will amplify noise exponentially, leading to the Gradient Explosion observed in legacy sovereign-to-unified transitions.\


nstead of relying on BatchNorm (which only scales activations), we apply Spectral Normalization directly to the weights of the consolidated node. This resizes the weight matrix W at every iteration:
Wnorm​=σ(W)W​

Why this is mandatory: Unlike LayerNorm, which can "homogenize" distinct economic signals, Spectral Normalization preserves the relative Rank of the features (keeping the Euro's influence distinct from the USD) while strictly capping the "math pressure" (Lipschitz constant) at 1.

To ensure the "Crossover Gate" (σ) doesn't create a sharp mathematical cliff, we add a penalty term to the Loss Function based on the Frobenius Norm of the Jacobian.
Losstotal​=Losstask​+λ∣∣∇x​Output(t)∣∣F​

This forces the transition between the legacy currencies and the unified index to be "smooth" in high-dimensional space, preventing the "Optimizer Spikes" that occur when the model's "inertia" (Momentum Warping) hits a structural discontinuity.


8. Final Verification Protocol

    Eigenvalue Tracking: Monitor the spectral radius of the GNN adjacency matrix during the transition window τ.

    Alpha-Consistency Check: Ensure the Power-Law decay of the eigenspectrum (α) remains within ±5% of the pre-transition state.

    Residual Variance Test: Verify that the "Whitening" process (PΣPT) accounts for at least 95% of the ancestral node variance.


Exapanding OLS for the projection matrix so there is no look ahead bais. 
    